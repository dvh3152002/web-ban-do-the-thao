<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../../partials/head') %>
</head>
<body>
  <%- include('../../partials/menu') %>
  <div class="manage-container">
      <%- include('../../partials/slider') %>
      <div class="manage-right">
          <div class="row">
              <div class="col-12"><h3>Người dùng</h3></div>
              <div class="col-md-12 mb-2">
                <a href="/userCreate" class="btn btn-primary float-end">Add</a>
              </div>
              <div class="col-md-12">
                <table class="table">
                    <thead>
                      <tr>
                        <th scope="col-2">Họ tên</th>
                        <th scope="col-2">Email</th>
                        <th scope="col-2">Action</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% for(let i=0;i<data.length;i++){ %>
                        <form action="/userDelete" method="post">
                          <tr>
                          <td class="col-3"><%= data[i].name %></td>
                          <td class="col-3"><%= data[i].email%></td>
                          <td class="col-2">
                            <a href="/userEdit/<%=data[i].id %>" class="btn btn-success">Sửa</a>
                              <input type="text" name="id" value="<%=data[i].id %>" hidden>
                              <button class="btn btn-danger">Xóa</button>
                          </td>
                        </tr>
                      </form>
                      <% } %>
                    </tbody>
                </table>
              </div>
              <nav>
                <ul class="pagination pagination-sm">
                  <%for(let i=0;i<count;i++){%>
                    <li class="page-item <%=page===i+1?'active':''%>" aria-current="page">
                      <a href="/userList/<%=i+1%>" class="page-link"><%=i+1%></a>
                    </li>
                  <%}%>
                </ul>
              </nav>
          </div>
        </div>
    </div>
</body>
</html>